# كيفية استخدام بيانات الإكسيل في النظام

## 🎯 الهدف
استخدام ملف الإكسيل `used_tables_export.xlsx` كمرجع رئيسي لبيانات:
- المتدربين (2571 متدرب)
- الأدوية (5 أدوية)
- سجل العيادة (23 مريض)
- الدورات والأقسام والكليات

---

## 📋 الملفات الرئيسية

### 1. `excel_data_reference.py` - مكتبة البحث
المكتبة الرئيسية التي تتعامل مع ملف الإكسيل

**الاستخدام:**
```python
from excel_data_reference import get_student_by_id, search_students

# البحث عن متدرب برقمه
student = get_student_by_id("2101024")
print(student['student_Name'])  # اسم المتدرب

# البحث النصي
results = search_students("محمد أحمد")
```

### 2. `/api/excel/*` - واجهة الويب
API endpoints للبحث من الويب

**الأمثلة:**
```javascript
// البحث عن متدرب
fetch('/api/excel/students/2101024')
  .then(r => r.json())
  .then(data => console.log(data.data))

// البحث عن أدوية
fetch('/api/excel/drugs/all')
  .then(r => r.json())
  .then(data => console.log(data.drugs))
```

---

## 🔍 حالات الاستخدام

### 1️⃣ إضافة متدرب في دورة تدريبية
عندما تقوم برئيس القسم بإضافة متدرب:
- أدخل رقم المتدرب (مثلاً: 2101024)
- سيتم البحث تلقائياً في الإكسيل
- ستظهر بيانات المتدرب: الاسم، التخصص، الكلية
- ستُحفظ هذه البيانات في سجل الدورة

### 2️⃣ البحث السريع عن متدرب
في أي صفحة، يمكنك البحث عن:
- الاسم الكامل
- رقم المتدرب
- التخصص
- الكلية

### 3️⃣ عرض بيانات كاملة للمتدرب
عند النقر على اسم متدرب:
- اسم كامل
- رقم المتدرب
- الكلية
- التخصص
- البيانات الأخرى

### 4️⃣ إدارة الأدوية
- قائمة جميع الأدوية المتوفرة
- بحث عن دواء محدد
- معلومات الدواء: الاسم التجاري والعام والقوة والشكل والكمية المخزنة

---

## 🛠️ التثبيت والتفعيل

### خطوة 1: تثبيت المكتبات المطلوبة
```bash
pip install openpyxl pandas
```

### خطوة 2: إضافة جدول قاعدة البيانات
```bash
python add_excel_reference_table.py
```

### خطوة 3: اختبار النظام
```bash
python test_excel_integration.py
```

### خطوة 4: تشغيل التطبيق
```bash
python -m uvicorn app.main:app --reload
```

---

## 📊 الإحصائيات

```
المتدربين: 2571
الأدوية: 5
مرضى العيادة: 23
الدورات: 6
الأقسام: 11
الكليات: 4
المستخدمين: 3
```

---

## 🔐 الأمان

- جميع API endpoints تتطلب تسجيل دخول
- يتم التحقق من صلاحيات المستخدم
- البيانات لا تُحفظ في قاعدة البيانات (من الإكسيل مباشرة)

---

## 🚀 الميزات المستقبلية

- [ ] مزامنة تلقائية لملف الإكسيل
- [ ] إضافة صفحات واجهة للبحث المتقدم
- [ ] ربط البيانات بشكل تلقائي عند إنشاء سجلات جديدة
- [ ] تقرير مقارنة بين الإكسيل وقاعدة البيانات

---

## ❓ الأسئلة الشائعة

### س: ماذا لو لم يوجد المتدرب في الإكسيل؟
**ج:** يمكن إضافة المتدرب بدون بيانات من الإكسيل. سيتم استخدام الرقم فقط.

### س: كيف أحدث بيانات المتدربين؟
**ج:** حدّث ملف `used_tables_export.xlsx` وأعد تشغيل التطبيق.

### س: هل البيانات محفوظة في قاعدة البيانات؟
**ج:** لا، البيانات تُقرأ من الإكسيل مباشرة. فقط الروابط تُحفظ في جدول `excel_data_references`.

### س: هل يؤثر حجم ملف الإكسيل على الأداء؟
**ج:** البيانات تُحمّل مرة واحدة عند بدء التطبيق، فالأداء سريعة جداً بعدها.

---

## 📞 التواصل والدعم
للمساعدة أو الإبلاغ عن مشاكل، تحقق من ملف `EXCEL_INTEGRATION.md` للمزيد من التفاصيل التقنية.
